#include <iostream>
#include <string>
#include <vector>

std::vector<std::string> solution(const std::string &s)
{
    std::vector<std::string> res{};

    for (int i = 0; i < s.size(); i++)
    {

    }

    return res;
}

int main()
{
    return 0;
}
